# Image-Classification-with-Neural-Networks
Image Classification with Neural Networks is a deep learning project built with TensorFlow and Keras that uses a Convolutional Neural Network (CNN) to classify images from the CIFAR-10 dataset into 10 categories. It demonstrates image preprocessing, model training, evaluation, and prediction for computer vision tasks.
